# Deployment Sync Marker

This file exists only to force a normal GitHub push event under `action_move/`, so the production deployment workflow can run through Actions rather than a local release script.
